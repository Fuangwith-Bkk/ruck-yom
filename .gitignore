# Secret Files
.env
.env.local
.env.production.local
!.env.example

# Device registry (contains real Tuya device IDs)
src/config/deviceRegistry.json
!src/config/deviceRegistry.example.json

# Dependencies
node_modules/

# OS & Editor Assets
.DS_Store
.vscode/
.idea/
.claude/
*.log

# Temp Files
npm-debug.log*
yarn-debug.log*

# VM-specific ops script (Oracle Cloud deployment control, not app code)
ruck-yom.sh

# Quiet-mode crash-recovery marker (runtime state, not app code)
quiet-state.json

# Daily รายงาน disabled marker (runtime state, not app code)
report-state.json

# Active bot role (dev/prod/dr) + learned groupIds (runtime state, not app code)
line-role-state.json
